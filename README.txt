Replace only this file in GitHub:

app/globals.css

This update adds a soft black-to-image gradient so the hero photograph blends naturally into the left panel rather than looking pasted on.

Vercel will redeploy automatically after the commit.
